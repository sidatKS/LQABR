# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
out/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# Environment & secrets — never commit these
.env
.env.local
.env.bak
**/.env.bak
.env.*.local
*.key
*.pem
service-account*.json
gcp-credentials*.json

# GCP / gcloud local state
.gcloud/

# Editor & OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Test coverage
coverage/
.nyc_output/
htmlcov/

# Logs

# Turborepo / build cache
.turbo/
.cache/

# ADK local dev artifacts (session store written by `adk web` / `adk run`)
.adk/

# Pytest
.pytest_cache/

# Real seed data — PII, never commit (kept locally only)
data/incoming/*
!data/incoming/.gitkeep

# Python

# Environment & secrets — never commit these

# GCP / gcloud local state

# Editor & OS

# Test coverage

# Logs

# Turborepo / build cache

# ADK local dev artifacts (session store written by `adk web` / `adk run`)

# Pytest

# Real seed data — PII, never commit (kept locally only)

# Python

# Environment & secrets — never commit these

# GCP / gcloud local state

# Editor & OS

# Test coverage

# Logs

# Turborepo / build cache

# ADK local dev artifacts (session store written by `adk web` / `adk run`)

# Pytest

# Email Agent runtime state (v2)
# Run state written at step 7 so late Mailgun events still resolve to a lead.
.runstate/
# Local tee of the four observability streams (LQABR_EMAIL_LOG_DIR).
.logs/
*_log.jsonl
# Scratch / to-delete artifacts
_to_delete/

# Claude Code local settings
.claude/
mcp/.env
scripts/dev/ngrok.env
ngrok/.env

# Hand-made archives — a zip of the tree is never the source of truth.
*.zip
*.tar.gz
