# agents/text_voice — DEV/TEST dependencies, on top of runtime.
#
# google-adk is the org's dev/test runtime standard (`adk web` — type
# 'test <id>' in it for the full end-to-end live test) and what the
# SERVICE_KIND=agent image (adk api_server) runs. It is deliberately NOT a
# runtime dependency: the production webhook image ships without it (see
# requirements.txt). Install for local work:
#     pip install -r requirements-dev.txt
google-adk==2.3.0
-r requirements.txt
