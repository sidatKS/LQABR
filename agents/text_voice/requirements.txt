# agents/text_voice — runtime dependencies (the whole set; no requirements-dev).
# lqabr_core is installed separately: pip install -e packages/lqabr_core
litellm>=1.50
requests>=2.31
fastapi>=0.111
uvicorn>=0.30
