# agents/text_voice — RUNTIME dependencies (what the production image installs).
#
# google-adk is NOT here on purpose (removed 2026-08-03, verified by booting
# the webhook with google-adk/google-genai uninstalled and exercising
# /healthz + /voice_agent/lead end-to-end — zero imports of it in the prod path):
# the production service is `uvicorn tools:app` behind the Agent Gateway;
# only adk_agent.py/agent.py import ADK and nothing imports THEM at runtime
# (the entrypoint imports `tools` top-level, so src/__init__.py never runs).
# ADK lives in requirements-dev.txt for the `adk web` dev/test runtime.
# Cuts roughly 200-300 MB from the image.
litellm>=1.50
requests>=2.31
fastapi>=0.111
uvicorn>=0.30
python-multipart>=0.0.9

# lqabr_core is installed separately: pip install -e packages/lqabr_core (repo root)
