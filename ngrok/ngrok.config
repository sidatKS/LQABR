# Local ngrok config (sourced by ngrok_run.sh). Env vars override (env > file).
# Static free domain reserved for the gateway (see docs/ngrok_setup.md).
: "${NGROK_DOMAIN:=armed-equal-share.ngrok-free.dev}"
: "${NGROK_PORT:=8080}"
: "${NGROK_LOG:=logs/ngrok/ngrok.log}"
